class Solution {
public:
    bool isPowerOfFour(int n) {
        // Must be positive, power of two, and set bit at correct position
        return n > 0 && (n & (n - 1)) == 0 && (n & 0x55555555);
    }
};
